# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/) 规范。
所有显著变更都会记录在此文件中。

---

## [4.4.0] - 2026-08-13

### 新增
- **分片管理子菜单**（菜单8）：自动恢复（扫描目录）/ 手动恢复（输文件名）/ 增加备用区（备份分片）/ 删除区（删分片）

---

## [4.3.3] - 2026-08-13

### 改动
- 分片恢复改为输入文件名自动读取（原来要手动输入几百位的id:value，不现实）
- 生成分片时显示分片目录和恢复方法

---

## [4.3.2] - 2026-08-13

### 修复
- 上链/查询请求被Cloudflare拦截(403)：urllib默认User-Agent被CF识别为爬虫，所有请求统一加 agent-keynl UA

---

## [4.3.1] - 2026-08-13

### 修复
- 菜单7"生成分片"调用已改名的 _input_password 导致 NameError，改为 _get_password

---

## [4.3.0] - 2026-08-13

### 新增
- **验证OTS存证**（菜单21/`keynl ots-verify`）：输入.ots证明文件本地验证比特币时间戳

---

## [4.2.0] - 2026-08-13

### 新增
- **OpenTimestamps 比特币存证**：上链时自动提交哈希到公共日历（a/b.pool.opentimestamps.org 等4个），生成 .ots 证明，证明"密钥在X时刻已存在"
- .ots 证明自动上传 IPFS

---

## [4.1.0] - 2026-08-13

### 新增
- **文件上链**（菜单20/`keynl chain-file`）：任意文件算哈希上链校验
- **IPFS 去中心化备份**：锚点自动同步到 IPFS，服务器挂了锚点仍在
- **区块链接**：每个区块记录前块哈希，防整段替换
- **ECC 签名**：每个锚点用服务器 ECC 私钥签名，防伪造

### 改动
- 链上记录含：原始哈希 + emoji表情 + 生成时间 + 前块哈希 + ECC签名 + IPFS地址

---

## [4.0.0] - 2026-08-13

### 新增
- **哈希→emoji表情映射**：SHA-256哈希可视化为8个emoji，篡改即表情变化
- **上链校验**（菜单16/`keynl chain`）：上传哈希到服务器生成比对页面
- **泄露查询**（菜单17/`keynl query`）：显示本地表情+链上链接比对
- **抹除式更新**（菜单18/`keynl wipe`）：输nlyes确认后删除所有密钥重新初始化
- scrypt新增"极高"档（64MB内存）

### 改动
- 菜单改为两排布局

---

## [3.9.0] - 2026-08-13

### 改动
- **初始化即环境绑定**：主密钥库 vault.enc 的加密密钥(derive_key)混入环境密钥，从 setpass 那一刻就绑定环境，而非仅导出 API 时
- setpass 时生成环境密钥并提示备份 env.key

---

## [3.8.0] - 2026-08-13

### 新增
- **环境密钥机制**：每个 keynl 安装实例生成独立随机密钥，导出的 API 文件绑定环境，其他环境无法解密

---

## [3.7.0] - 2026-08-13

### 安全修复
- **API导出去明文密钥**：原设计打印明文API密钥，任何人可解密。改为硬件绑定加密，文件复制到别处即失效

### 改动
- 导出加密类型改为：本机绑定（硬件指纹）/ 密码加密（仅主密码）
- api-get 自动识别加密类型

---

## [3.6.0] - 2026-08-13

### 新增
- **多字段存储**：支持一个密钥存多个字段（如腾讯云 SecretId+SecretKey），用 `字段名=值` 多行输入
- **自定义分片**：分片方案支持自定义 n,k（如 7,3）
- **导出API**：菜单15 / `keynl export`，选密钥导出加密文件给 AI 调用，支持 Fernet 随机密钥 / 密码两种加密

---

## [3.5.0] - 2026-08-13

### 新增
- **免密授权**：菜单13 / `keynl authorize`，本次终端窗口内免密操作
- **关于作者**：菜单14 / `keynl about`

### 修复
- 密码输入错误不再崩溃，改为友好提示

---

## [3.4.0] - 2026-08-13

### 新增
- **检查更新**：菜单选项 12 / 命令 `keynl update`，自动检测 GitHub 最新版并更新

### 改动
- **命令改名**：`keymgr` → `keynl`（所有文件、安装脚本、文档统一）

---

## [3.3.0] - 2026-08-13

### 新增
- **交互式菜单**：输入 `keynl` 进入宝塔式菜单，输数字 0-11 选择功能
- **可配置加密强度**：scrypt 三档（快速 8MB / 标准 16MB / 高强度 16MB+迭代翻倍），修改后自动重新加密数据
- **可配置分片数量**：Shamir 支持 3-of-5 / 3-of-7 / 5-of-7 / 5-of-9
- **version 命令**：`keynl -v` / `keynl version` 查看版本

### 修复
- 高强度档 N=65536 触发 OpenSSL 32MB 内存限制，改用 (n,r) 元组档位规避

---

## [3.2.0] - 2026-08-13

### 修复
- **Shamir 素数溢出**：`prime` 从 `2^127-1` 提升到 `2^521-1`，修复中文/长密码（>16字节）恢复乱码的严重 bug
- **内核指纹不稳定**：`get_kernel()` 改用 `platform.release()`（稳定版本号），避免内核小更新导致硬件指纹变化、密钥库打不开
- Shamir 分片增加长度检查（密码最多 65 字节）

---

## [3.1.0] - 2026-08-13

### 新增
- **跨平台支持**：Windows / Linux / Android(Termux) 三平台
- **install.ps1**：Windows 一键安装脚本
- **默认路径适配**：Linux `~/.agent-keynl` · Windows `%APPDATA%\agent-keynl` · Termux `~/.agent-keynl`

### 改动
- **scrypt 替代 Argon2**：用 Python 内置 `hashlib.scrypt` 替代 `argon2-cffi`，消除编译依赖，安装不再卡住
- **ECC 惰性初始化**：不再模块加载时写文件（修复 Termux 只读文件系统报错）
- 一键安装脚本改用 `mktemp` 跨平台临时文件

### 修复
- Termux 安装到 `$PREFIX/bin`（修复 `command not found`）
- install.sh 增加完整使用说明

---

## [3.0.0] - 2026-08-13

### 新增
- **HSM 自动适配**：TPM 2.0 / 云 KMS / 软件指纹 三级自动检测
- **密码管理命令**：`setpass`（设密码）、`changepass`（改密码）、`recover`（分片恢复）
- **一键安装**：`curl install.sh | bash`

### 改动
- 去品牌化：`祈棂密钥库` → `agent-keynl`

---

## [2.0.0] - 2026-08-10

### 新增
- **Argon2id** 内存密集型密钥派生（抗 GPU/ASIC）
- **Shamir(3,5)** 门限分片，任意 3 份恢复主密码
- **mlock** 内存锁，防 swap 泄漏

---

## [1.0.0] - 2026-08-09

### 新增
- **七层加密**：scrypt/PBKDF2 + SHA-384 + 硬件绑定 + Fernet AES + ECC P-384 + 文件权限
- **硬件指纹绑定**：MAC + 机器 ID + 主机名 + 内核版本
- **假文件完整性校验**：删错诱饵文件即锁死
- **伪装目录**：伪装成 Node.js 项目

---

## 版本号说明

| 类型 | 说明 | 示例 |
|:---|:---|:---|
| 主版本号 | 不兼容的重大变更 | 3.x → 4.x |
| 次版本号 | 向后兼容的新功能 | 3.1 → 3.2 |
| 修订号 | 向后兼容的 bug 修复 | 3.1.1 |
